# Streamlit
streamlit==1.49.1

# PyTorch stack for Python 3.13 (CPU wheels available)
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# (Optional) If resolution ever fails on your runner, add this line:
# --extra-index-url https://download.pytorch.org/whl/cpu

# If you are NOT using TensorFlow, remove it to speed up build & save RAM.
# tensorflow==2.20.0
# tensorboard==2.20.0

# (keep anything else you actually use, e.g.)
pillow==11.3.0
numpy==2.3.3
pandas==2.3.2
